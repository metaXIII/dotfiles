# WSL

## Installation et configuration de zsh

```bash
sudo apt update && sudo apt install zsh && mkdir ~/.config/zsh && curl -s https://ohmyposh.dev/install.sh | bash -s -- -d ~/.config/zsh
```

## Conf
[env](./env)
